- We want to create a drag and drop ui for creating declarative agents
- We want building blocks to be available for users to drag and drop
- Create a canvas which allows one to build a graph and connect the nodes
- Clicking on each node should open a UI view of the node
- Each node/edge should allow to view the code YAML for that node
- There should also be a way to view the code YAML for the whole system


- We want to create this prototype inside figma
- Create a detailed prompt with all the information of various types of nodes that we have along with the schema
- Figma should have all the knowledge about the types of nodes, configs available etc